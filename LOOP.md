# LOOP — how a pass writes

1. Pick the **least-reviewed** Garden idea (equal weight).
2. Run three functions: Content + (Profit if commercial else Systems) + Hybrid.
3. Stamp **before → after**. That is the revision.
4. Prepend PASS_LOG.md. Commit `loop: pass #<n> <idea> × <fn>`.
5. Pages stay read-only until explicit go.
6. Open question: route by overlap, then the same three functions.

Cap 11 per session. Halt is first-class. GROUNDED gets edits; it does not get a sales pitch.
