# Control plane
**Locked 2026-09-11.** Public-safe.

The OS is the files. This conversation is a pipe, not the product.

1. Ryan talks. A model proposes a file.
2. A commit on `cooldad777/Rsee` (or `see-r-os` if private) is the switch.
3. Pages move only on an explicit go.
4. If this chat dies, load `00_START.md` + `GARDEN.md` + `PASS_LOG.md` in any model. The desk continues.
5. Halt is a feature. Ease is not a reason to skip the commit.

Nimble means: change one file, not stand up a new host.
